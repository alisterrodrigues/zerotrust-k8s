# Custom golangci-lint plugin build configuration.
# The logcheck plugin has been removed — it requires CGO plugin compilation
# which is fragile in CI environments. Standard linters in .golangci.yml
# provide sufficient coverage for this project.
#
# To re-enable plugins in the future, add entries under the 'plugins' key.
# See: https://golangci-lint.run/plugins/module-plugins/
version: v2.8.0
# plugins: (none)
