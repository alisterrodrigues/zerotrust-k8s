# E2E tests are Kubebuilder-scaffolded boilerplate (//go:build e2e) with no
# project-specific tests. They require a built Docker image and make deploy
# against a live cluster, which is not part of this project's CI scope.
# This workflow is intentionally disabled. Unit and integration tests run in
# the main CI workflow (test.yml).
name: E2E Tests (disabled)

on:
  workflow_dispatch: # Only runs when manually triggered — never on push/PR

jobs:
  placeholder:
    name: E2E tests disabled (see workflow comments)
    runs-on: ubuntu-latest
    steps:
      - run: echo "E2E tests are out of scope for this project. See test.yml for CI."
